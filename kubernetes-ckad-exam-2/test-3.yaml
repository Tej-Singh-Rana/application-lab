
kubectl create deploy beta-apps --image=nginx --dry-run=client -oyaml > test3.yaml

affinity:
  nodeAffinity:
    requiredDuringSchedulingIgnoredDuringExecution:
      nodeSelectorTerms:
      - matchExpressions:
        - key: app_type
          values: ["beta"]
          operator: In